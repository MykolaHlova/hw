const user = require('./user');
const house = require('./house');
const render = require('./render');
const auth = require('./auth');


module.exports = {
    user,
    house,
    render,
    auth
};

